## Vercel / local configuration
# Use your deployed backend URL in Vercel (Production environment variables).
# For local dev, leave unset to use the localhost fallback in the app.
VITE_API_BASE_URL=http://localhost:5000

